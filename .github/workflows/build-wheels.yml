name: Build Python Wheels

on:
  push:
    branches:
      - main
  # Эта строка добавляет кнопку для ручного запуска
  workflow_dispatch:

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }} for Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ['3.7', '3.8', '3.9', '3.10', '3.11', '3.12']

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      # --- Инструменты сборки для конкретных платформ ---
      - name: Set up MSVC for Windows
        # Этот шаг запустится ТОЛЬКО на Windows
        if: runner.os == 'Windows'
        uses: microsoft/setup-msvc-dev@v4
        with:
          architecture: 'x64'

      - name: Install build dependencies on Linux
        # Этот шаг запустится ТОЛЬКО на Linux
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y build-essential

      # --- Общие шаги сборки для всех платформ ---
      - name: Install Python build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install wheel build cython numpy

      - name: Build wheel
        # Эта команда скомпилирует wheel для текущей ОС и версии Python
        run: python -m build --wheel

      - name: Store artifact
        uses: actions/upload-artifact@v4
        with:
          # Имя артефакта будет включать ОС и версию Python для удобства
          name: wheels-${{ matrix.os }}-py${{ matrix.python-version }}
          path: dist/*.whl
